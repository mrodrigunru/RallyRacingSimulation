
/**
 * Clase para inciar los datos de la simulacion
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class InitData
{
    // instance variables - replace the example below with your own
    private Organizacion org = Organizacion.getInstance();

    /**
     * Constructor for objects of class InitData
     */
    public InitData()
    {
        initData();
    }

   /**
    * Metodo que inicializa las variables de la Organizacion
    * 
    * @param (no params needed)
    * @return (nothing)
    */
   private void initData(){
       //inicializar segun el doc del CV
    }
}
